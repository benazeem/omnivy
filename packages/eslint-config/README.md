# `@repo/eslint-config`

Shared ESLint configuration for Omnivy workspace packages.

Use this package from workspace apps/packages that need the same baseline lint rules. Keep changes conservative because they affect both the web app and extension.
